import test from 'node:test';import assert from 'node:assert/strict';import DialogueEngine from '../../../resources/js/game/games/role-play/dialogue/DialogueEngine.js';import RelationshipSystem from '../../../resources/js/game/games/role-play/relationships/RelationshipSystem.js';
function context(){return{flags:new Set(),variables:{},relationships:new RelationshipSystem(),inventory:{count:()=>0,add:()=>{},remove:()=>true,addCoins:()=>{}},quests:{start:()=>true,status:()=> 'not-started'},jobs:{addXp:()=>0,level:()=>0},emit:()=>true};}
test('dialogue runner filters choices and executes actions',async()=>{const c=context();const engine=new DialogueEngine({context:c,definitions:[{id:'d',start:'a',nodes:{a:{text:'A',choices:[{id:'locked',text:'L',condition:{type:'flag',key:'ok',operator:'truthy'},next:'b'},{id:'open',text:'O',actions:[{type:'setFlag',key:'ok'},{type:'relationship',npcId:'tiam',amount:5}],next:'b'}]},b:{text:'B',end:true}}}]});await engine.start('d');assert.deepEqual(engine.snapshot().choices.map((x)=>x.id),['open']);await engine.choose('open');assert.equal(c.flags.has('ok'),true);assert.equal(c.relationships.get('tiam'),5);assert.equal(engine.active(),false);});
test('dialogue engine rejects unavailable choices',async()=>{const engine=new DialogueEngine({context:context(),definitions:[{id:'d',start:'a',nodes:{a:{text:'A',choices:[]}}}]});await engine.start('d');await assert.rejects(()=>engine.choose('missing'));});
