import test from 'node:test';import assert from 'node:assert/strict';import RelationshipSystem,{relationshipRank} from '../../../resources/js/game/games/role-play/relationships/RelationshipSystem.js';import JobSystem from '../../../resources/js/game/games/role-play/jobs/JobSystem.js';import ScheduleSystem from '../../../resources/js/game/games/role-play/npc/ScheduleSystem.js';import NpcMemory from '../../../resources/js/game/games/role-play/npc/NpcMemory.js';
test('relationship values clamp and map to stable ranks',()=>{const r=new RelationshipSystem();r.adjust('tiam',40);assert.equal(r.rank('tiam'),'friend');r.adjust('tiam',100);assert.equal(r.get('tiam'),100);assert.equal(relationshipRank(-60),'hostile');});
test('job system unlocks levels from definition thresholds',()=>{const jobs=new JobSystem({definitions:[{id:'cafe',title:'Cafe',levels:[{level:1,xp:50},{level:2,xp:100}]}]});jobs.unlock('cafe');jobs.addXp('cafe',120);assert.equal(jobs.level('cafe'),2);});
test('schedule wraps midnight and NPC memory keeps important bounded entries',()=>{const s=new ScheduleSystem([{npcId:'n',entries:[{minute:60,activity:'sleep'},{minute:480,activity:'work'}]}]);assert.equal(s.resolve('n',30).activity,'work');assert.equal(s.resolve('n',500).activity,'work');const m=new NpcMemory({limit:2});m.remember('a',{},1);m.remember('b',{},5);m.remember('c',{},3);assert.deepEqual(m.snapshot().map((e)=>e.type),['b','c']);});
