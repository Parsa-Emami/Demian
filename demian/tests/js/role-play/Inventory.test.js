import test from 'node:test';import assert from 'node:assert/strict';import ItemRegistry from '../../../resources/js/game/games/role-play/inventory/ItemRegistry.js';import InventorySystem from '../../../resources/js/game/games/role-play/inventory/InventorySystem.js';import EquipmentSystem from '../../../resources/js/game/games/role-play/inventory/EquipmentSystem.js';
const registry=()=>new ItemRegistry([{id:'coffee',title:'Coffee',stackSize:3},{id:'tool',title:'Tool',stackSize:1,equipSlot:'tool'}]);
test('inventory stacks items, enforces capacity and removes atomically',()=>{const r=registry(),i=new InventorySystem({registry:r,capacity:2});i.add('coffee',5);assert.equal(i.stacks.length,2);assert.equal(i.count('coffee'),5);assert.equal(i.remove('coffee',4),true);assert.equal(i.count('coffee'),1);assert.equal(i.remove('coffee',2),false);});
test('inventory transactions roll back on failure',()=>{const r=registry(),i=new InventorySystem({registry:r,capacity:2});i.add('coffee',2);assert.throws(()=>i.transact([{type:'remove',itemId:'coffee',amount:1},{type:'remove',itemId:'tool',amount:1}]));assert.equal(i.count('coffee'),2);});
test('equipment only accepts owned equippable items',()=>{const r=registry(),i=new InventorySystem({registry:r}),e=new EquipmentSystem({inventory:i,registry:r});assert.throws(()=>e.equip('tool'));i.add('tool');e.equip('tool');assert.equal(e.get('tool'),'tool');});
