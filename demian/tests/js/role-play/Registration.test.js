import test from 'node:test';import assert from 'node:assert/strict';import { GAME_DEFINITIONS } from '../../../resources/js/game/registry/GameDefinitions.js';import { GAME_CATALOG } from '../../../resources/js/game/catalog/GameCatalog.js';import { INPUT_CONTEXTS } from '../../../resources/js/game/input/InputContexts.js';
test('Role Play is a playable lazy game with isolated semantic input',()=>{const d=GAME_DEFINITIONS['role-play'];assert.equal(d.inputContext,'ROLE_PLAY');assert.equal(d.metadata.phase,7);assert.equal(d.metadata.dataDrivenDialogue,true);assert.equal(GAME_CATALOG.find((g)=>g.id==='role-play').available,true);assert.ok(INPUT_CONTEXTS.ROLE_PLAY.actions.toggleInventory);assert.ok(INPUT_CONTEXTS.ROLE_PLAY.actions.toggleQuests);});
