function freeze(value){if(!value||typeof value!=='object'||Object.isFrozen(value))return value;Object.values(value).forEach(freeze);return Object.freeze(value);}function clone(v){return typeof structuredClone==='function'?structuredClone(v):JSON.parse(JSON.stringify(v));}
export function createRolePlaySnapshot(source){return freeze(clone({version:1,tick:source.tick??0,worldTime:source.worldTime??480,player:source.player,dialogue:source.dialogue,quests:source.quests,inventory:source.inventory,equipment:source.equipment,relationships:source.relationships,jobs:source.jobs,npcs:source.npcs,flags:[...(source.flags??[])],status:source.status??''}));}
export function createRolePlaySaveState(source){return clone({version:2,worldTime:source.worldTime,player:source.player,flags:[...source.flags],variables:source.variables,inventory:source.inventory.export(),equipment:source.equipment.snapshot(),relationships:source.relationships.snapshot(),jobs:source.jobs.export(),quests:source.quests.export(),dialogue:source.dialogue.export(),npcMemory:Object.fromEntries([...source.npcs].map(([id,n])=>[id,n.brain.memory.snapshot()]))});}
