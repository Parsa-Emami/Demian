import BaseModifier from './BaseModifier.js'; export default class FogModifier extends BaseModifier { apply(runtime){ super.apply(runtime); runtime.fogDensity=Math.max(runtime.fogDensity,Math.max(0,this.value)); runtime.viewDistanceMultiplier=Math.max(.25,1-this.value*7); } revert(runtime){ if(this.applied){runtime.fogDensity=0.018;runtime.viewDistanceMultiplier=1;} super.revert(runtime); } }
